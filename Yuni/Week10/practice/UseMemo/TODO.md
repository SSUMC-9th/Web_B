# TODO

## 1. 다면러시 배열 (의존성 배열) 을 신중하게 설정
- 만약 `count`를 넣지 않았다면 최신 값이 반영되지 않을 수 있었다.
- 너무 많은 변수를 넣은 경우는, 최적화 효과가 사라질 수 있다.

## 2. useCallback Tradeoff
- useCallback 자체가 메모리 메모이제이션이기 때문에, 메모리를 많이 사용한다가스.
- 정말 필요한 때 사용하기.
